const initState = {
  users: [],
  loadding: false,
};

const UsersReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UsersReducer;
